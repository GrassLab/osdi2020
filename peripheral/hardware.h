#ifndef HARDWARE
#define HARDWARE

#include <stddef.h>
unsigned int hardware_info_VC_base ();
unsigned int hardware_info_revision ();
size_t hardware_info_memory_size ();
#endif /* ifndef HARDWARE */
