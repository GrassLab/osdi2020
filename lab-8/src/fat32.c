#include "fat32.h"
#include "sdlib.h"
#include "printf.h"

void setup_mount() {
    char buf[512];
    printf("OK\n");
    // readblock (0, buf);
    // printf("%s\n", buf);
    // printf("%s\n", buf);
}